# feedback-trail-color — preset card

AnimationTime advances the trail; BlendMode = Screen and Threshold = (drop)
keep the trail bright where new pixels arrive. Loops every 2 seconds.

Aliases: color-trail.

Required: AnimationTimeNode, BlendModeNode, OpacityNode, ThresholdNode.

Best for: title-bar reveals, bloom trails, kinetic-typography ornaments.
